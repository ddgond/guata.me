Add a script that integrates with Vali to generate maps for each region of each quiz
  Can use nix-shell to include it as a dependency. We can create a new repo for the nix definition for Vali if needed and I can push it to GitHub.
